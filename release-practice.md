iOS
Make cosmetic change in ios-brandmessenger-ui repository
Run build script from parent directory
Update version in podspec to beta version (e.g., 1.3.1-beta.1)
Verify CocoaPods trunk access with pod trunk me
Publish beta to trunk with pod trunk push --allow-warnings
Create or use existing Brand Messenger test app
Update test app Podfile to use specific beta version
Build test app and verify cosmetic change is visible
Note: Published versions cannot be deleted from CocoaPods trunk